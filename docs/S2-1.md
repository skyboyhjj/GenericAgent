**任务编号**：S2-1  
**任务名称**：实现 XiMingClient 数据同步客户端  
**优先级**：P0  

工作区目录：`D:\GenericAgent`  
参考文档：`docs/产品体验定义书.md`, `docs/技术规格书_v0.3.0.md`, `docs/工程迭代计划_v1.1.0.md`

**已完成任务**：
- Sprint 1 全部完成。`core/soul.py` 提供 `Soul` 类、`to_system_prompt()`、`check_moral_boundary()`。`core/soul_prompts.py` 提供 `inject_soul_prompt()`。`core/initializer.py` 提供 `InitializationHandler`。`launch_huihui.pyw` 为启动入口。

**操作文件**：
- `memory/gateway/__init__.py`（新建空文件）
- `memory/gateway/adapters/__init__.py`（新建空文件）
- `memory/gateway/adapters/ximing_adapter.py`（新建，实现 XiMingClient 全部功能）
- `memory/ximing_client.py`（新建，作为兼容层：from memory.gateway.adapters.ximing_adapter import XiMingAdapter as XiMingClient）
- `memory/verify_s2_1.py`（新建验证脚本）

**实现要求**：
1. 实现 `XiMingClient` 类，基地址 `http://127.0.0.1:9876/api/v1`
2. `check_connectivity()` 方法：检测小程序 HTTP 服务是否可达，返回 `ConnectivityStatus`
3. `sync_history()` 方法：GET `/api/v1/history`，拉取全部历史修行数据
4. `sync_today()` 方法：GET `/api/v1/today`，拉取今日修行数据
5. 数据标准化为 `NormalizedDailyData` 格式（含 `date`, `algorithm_version`, `morning_intent`, `events`, `evening_reflection`）
6. 错误处理：超时5s静默降级，4xx记录日志，5xx指数退避重试最多3次

**关键约束**：
- 数据仅在 127.0.0.1 本地传输
- 所有同步请求标注 `algorithm_version`
- 修行数据只读，永不回写小程序
- 不改动 GenericAgent 核心文件

**验收标准**：
- `memory/ximing_client.py` 文件创建成功
- `check_connectivity()` 可达返回 True，不可达返回 False，不抛异常
- `sync_history()` 和 `sync_today()` 返回结构化 `SyncResult`
- `verify_s2_1.py` 全部测试通过
- 已有测试 verify_soul.py, verify_s1_2.py, verify_s1_3.py, verify_s1_5.py 仍全部通过